def wm (w):
    if len(w)==1:
        return w[0]
    if len(w)==2:
        if pond(w[0])<pond(w[1]):
            return w[0]
        else:
            return w[1]
        e=w[0]
        sum(s)=sum(d)=0
        ws=[]
        wd=[]
        for x in w:
            if val[x]<val[e]:
                ws.append(x)
                sums+=pond(x)
        if val(x)>val(e):
            wd.append(x)
            sumd+=pond(x)
        if sums<0,5 and sumd<=0.5:
            return e
        if sumd>0.5
            pond(e)=sums
            wd.append(e)


